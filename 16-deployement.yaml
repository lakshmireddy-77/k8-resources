apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  # replica set lables
  labels:
    purpose: deployment-demo
    project: roboshop
    app: nginx
spec:
  replicas: 2
  selector:
    matchLabels:
      purpose: deployment-demo
      project: roboshop
      app: nginx
  # this is pod defination
  template:
    metadata:
      labels:
        purpose: deployment-demo
        project: roboshop
        app: nginx
    spec:
      containers:
        - name: nginx
          image: nginx:latest